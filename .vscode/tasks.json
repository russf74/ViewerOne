{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "ViewerOne: sync from GitHub",
      "type": "shell",
      "command": "git fetch origin main && git reset --hard origin/main && powershell -NoProfile -ExecutionPolicy Bypass -File scripts/launch-viewerone.ps1 -FixOnly",
      "options": { "cwd": "${workspaceFolder}" },
      "runOptions": { "runOn": "folderOpen" },
      "problemMatcher": []
    }
  ]
}
